import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Si en el futuro necesitás algo de images/domains, etc., se agrega acá.
};

export default nextConfig;
